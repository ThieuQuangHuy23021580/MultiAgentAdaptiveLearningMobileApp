import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF004AC6);
  static const primaryContainer = Color(0xFF2563EB);
  static const primaryFixed = Color(0xFFDBE1FF);

  // Background
  static const scaffold = Color(0xFFF4F5F7);
  static const background = Color.fromARGB(255, 233, 234, 237);
  static const surface = Color(0xFFF8F9FF);

  static const surfaceLowest = Color(0xFFFFFFFF);
  static const surfaceLow = Color(0xFFEFF4FF);
  static const surfaceContainer = Color(0xFFE5EEFF);
  static const surfaceHigh = Color(0xFFDCE9FF);
  static const surfaceHighest = Color(0xFFD3E4FE);

  // Text
  static const onSurface = Color(0xFF0B1C30);
  static const secondary = Color(0xFF5C5E62);

  // Border
  static const outline = Color(0xFF737686);
  static const outlineVariant = Color(0xFFC3C6D7);

  // Success
  static const success = Color(0xFF2E7D32);

  // Warning
  static const warning = Color(0xFFFF9800);

  // Error
  static const error = Color(0xFFBA1A1A);
  static const errorContainer = Color(0xFFFFDAD6);

  // Glass
  static const glass = Color.fromRGBO(255, 255, 255, 0.70);

  static const shadow = Color.fromRGBO(0, 0, 0, 0.06);
}
